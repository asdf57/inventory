all:
  children:
    coords:
      hosts:
        t3:
    infras:
      hosts:
        t2:
  hosts:
    t2:
      ansible_host: 0.0.0.1
      ansible_port: 1
      ansible_user: root
      primary_mac: string
    t3:
      ansible_host: 0.0.0.1
      ansible_port: 1
      ansible_user: root
      primary_mac: string
