all:
  children:
    coords:
      hosts:
        test_host:
    infras:
      hosts:
        host1:
  hosts:
    host1:
      ansible_host: 0.0.0.1
      ansible_port: 1
      ansible_user: root
      primary_mac: string
    test_host:
      ansible_host: 0.0.0.0
      ansible_port: 1
      ansible_user: root
      primary_mac: string
